import os
import sys


def checkForPairOfTwoLetters(line):
    for i in range(len(line) - 2):
        for b in range(len(line) - 2):
            continue


path = os.path.join(sys.path[0], 'input.txt')
test = open(path)
content = test.read()
test.close()
content_lines = content.splitlines()
niceLines = 0


for line in content_lines:
    continue
